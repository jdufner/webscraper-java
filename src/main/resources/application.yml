spring:
  application:
    name: webscraper
  profiles:
    active: heise
selenium:
  chrome:
    options:
      - --disable-search-engine-choice-screen
      - --lang=de
      - --disable-notifications
      - --disable-infobars
      - --disable-extensions
#      - --headless
#      - --incognito
#      - --windows-size=1200x600
#      - --disable-gpu
#      - --disable-web-security
